# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is in its initial state — no source code has been added yet.

## MCP Servers

A `.mcp.json` is configured at the root with the GitHub MCP server (`@modelcontextprotocol/server-github`), enabling GitHub operations (issues, PRs, files, etc.) directly from Claude Code.
